# === DataSource (dentro de Docker el host es "mysql") ===
spring.datasource.url=jdbc:mysql://mysql:3306/mydatabase?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=rootpassword
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# === JWT (access token propio) ===
app.jwt.secret=${JWT_SECRET}
app.jwt.exp-min=60

# === Refresh token (cookie httpOnly propia) ===
jwt.refresh.secret=super-larga-y-separada-de-la-del-access-token-para-dev
jwt.refresh.ttl-seconds=2592000

# === Cookies (ajusta por entorno) ===
app.cookies.secure=false
app.cookies.same-site=Lax
app.cookies.path=/
app.cookies.domain=

# === Rate limiting (Bucket4j) ===
app.rate-limit.auth.capacity=10
app.rate-limit.auth.period-seconds=60
app.rate-limit.citas.capacity=30
app.rate-limit.citas.period-seconds=60

spring.flyway.enabled=false

# === Actuator (opcional) ===
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# =========================
# Agenda (slots de citas)
# =========================
# --- Agenda / Slots ---
agenda.timezone=America/Mexico_City
agenda.open-hour=9           
agenda.close-hour=19         
agenda.slot-size-min=15      
agenda.buffer-between-min=5  
agenda.min-advance-min=0     
agenda.max-advance-days=30   


# === Firebase Admin ===
# No requiere properties adicionales si usas GOOGLE_APPLICATION_CREDENTIALS
# (lo inyectamos por variable de entorno y montamos el JSON por volumen)
